<% @shorturls.each do |url| %>
	<br>
	<%= link_to(url.longurl, "http://www.#{url.longurl}") %>
	<br>
	turns into
	<br>
	<b> Short url&nbsp;</b><%= link_to("http://localhost:3000/#{url.id}", shorturls_path) %>
	<br>
<% end %> 